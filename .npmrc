# Limit npm to download only macOS binaries for optional dependencies (esbuild)
# This reduces node_modules size but limits portability
# Remove this file if you need Linux/Windows support or CI/CD compatibility
supportedArchitectures.cpu[]=arm64
supportedArchitectures.cpu[]=x64
supportedArchitectures.os[]=darwin
