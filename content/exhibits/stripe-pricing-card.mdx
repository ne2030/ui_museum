---
title: Stripe Pricing Card
slug: stripe-pricing-card
description: A beautiful pricing card component inspired by Stripe's clean design language
source:
  designer: Stripe Design Team
  designerUrl: https://stripe.com/design
  company: Stripe
  originalUrl: https://stripe.com/pricing
  designSystem: Stripe Design System
theme:
  background:
    type: gradient
    value: "linear-gradient(135deg, #f6f9fc 0%, #e9ecef 100%)"
  colors:
    primary: "#635bff"
    secondary: "#7a73ff"
    surface: "#ffffff"
    text: "#1a1a1a"
    muted: "#6b7280"
    border: "#e5e7eb"
    accent: "#635bff"
  typography:
    heading:
      family: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Helvetica, Arial, sans-serif"
      weight: 600
      letterSpacing: "-0.02em"
    body:
      family: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Helvetica, Arial, sans-serif"
      weight: 400
  radius:
    style: rounded
    values:
      sm: "8px"
      md: "12px"
      lg: "16px"
      full: "9999px"
  effects:
    shadows: soft
  interactions:
    hover:
      scale: 1.02
      lift: -4
      timing: 300
      easing: ease-out
    click:
      scale: 0.98
      feedback: press
  animations:
    enter:
      type: spring
      duration: 500
  meta:
    service: Stripe
    designSystem: Stripe Design System
    era: "2024"
    philosophy: "Clean, trustworthy, purple accents signal action. Generous whitespace creates breathing room."
    changes: "Evolved from darker purples to the current vibrant indigo. Increased border radius over time."
tags:
  - pricing
  - card
  - saas
  - minimal
category: cards
featured: true
---

export function StripePricingCard() {
  return (
    <div className="w-full max-w-sm mx-auto" style={{ fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Helvetica, Arial, sans-serif" }}>
      <div
        className="bg-white rounded-2xl p-8 shadow-lg border border-gray-100"
        style={{
          boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1)',
        }}
      >
        {/* Plan badge */}
        <div className="inline-block px-3 py-1 bg-purple-50 text-purple-600 text-sm font-medium rounded-full mb-4">
          Most popular
        </div>

        {/* Plan name */}
        <h3 className="text-2xl font-semibold text-gray-900 mb-2">
          Professional
        </h3>

        {/* Description */}
        <p className="text-gray-500 mb-6">
          For growing businesses that need more power.
        </p>

        {/* Price */}
        <div className="flex items-baseline gap-1 mb-6">
          <span className="text-5xl font-bold text-gray-900">$79</span>
          <span className="text-gray-500">/month</span>
        </div>

        {/* CTA Button */}
        <button
          className="w-full py-3 px-4 bg-[#635bff] text-white font-medium rounded-lg hover:bg-[#5851ea] transition-colors mb-6"
        >
          Start free trial
        </button>

        {/* Features */}
        <ul className="space-y-3">
          {[
            'Unlimited products',
            'Advanced analytics',
            'Priority support',
            'Custom integrations',
            'Team collaboration',
          ].map((feature, i) => (
            <li key={i} className="flex items-center gap-3 text-gray-600">
              <svg className="w-5 h-5 text-[#635bff]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
              </svg>
              {feature}
            </li>
          ))}
        </ul>
      </div>
    </div>
  );
}

<StripePricingCard />
