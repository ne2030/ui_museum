---
title: Linear Command Menu
slug: linear-command-menu
description: A command palette inspired by Linear's keyboard-first design
source:
  designer: Linear Team
  designerUrl: https://linear.app
  company: Linear
  originalUrl: https://linear.app
  designSystem: Linear Design
theme:
  background:
    type: color
    value: "#0a0a0b"
  colors:
    primary: "#5e6ad2"
    secondary: "#4752c4"
    surface: "#1c1c1e"
    text: "#ffffff"
    muted: "#8b8b8b"
    border: "#2a2a2c"
    accent: "#5e6ad2"
  typography:
    heading:
      family: Inter
      weight: 500
    body:
      family: Inter
      weight: 400
    mono:
      family: JetBrains Mono
      weight: 400
  radius:
    style: subtle
    values:
      sm: "4px"
      md: "6px"
      lg: "8px"
      full: "9999px"
  effects:
    shadows: none
    glow:
      enabled: true
      color: "#5e6ad2"
      spread: 20
  interactions:
    hover:
      scale: 1.0
      colorShift: "#5e6ad2"
      timing: 150
    focus:
      ring:
        color: "#5e6ad2"
        width: 2
  animations:
    enter:
      type: scale
      duration: 200
  meta:
    service: Linear
    designSystem: Linear Design
    era: "2024"
    philosophy: "Speed and precision, dark-first, keyboard-centric. Every millisecond matters."
    changes: "Refined the glow effects and reduced visual noise over time. Focus on content density."
tags:
  - command
  - menu
  - dark
  - keyboard
category: navigation
featured: true
---

export function LinearCommandMenu() {
  const commands = [
    { icon: '/', label: 'Create new issue', shortcut: 'C' },
    { icon: '#', label: 'Search issues', shortcut: '/' },
    { icon: '@', label: 'Assign to...', shortcut: 'A' },
    { icon: '!', label: 'Set priority', shortcut: 'P' },
    { icon: '~', label: 'Change status', shortcut: 'S' },
  ];

  return (
    <div className="w-full max-w-lg mx-auto">
      <div
        className="bg-[#1c1c1e] rounded-lg border border-[#2a2a2c] overflow-hidden"
        style={{
          boxShadow: '0 0 0 1px rgba(94, 106, 210, 0.1), 0 25px 50px -12px rgba(0, 0, 0, 0.5)',
        }}
      >
        {/* Search input */}
        <div className="flex items-center gap-3 px-4 py-3 border-b border-[#2a2a2c]">
          <svg className="w-5 h-5 text-[#8b8b8b]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
          <input
            type="text"
            placeholder="Type a command or search..."
            className="flex-1 bg-transparent text-white placeholder-[#8b8b8b] outline-none text-sm"
          />
          <kbd className="px-2 py-1 text-xs text-[#8b8b8b] bg-[#2a2a2c] rounded">esc</kbd>
        </div>

        {/* Commands list */}
        <div className="py-2">
          <div className="px-4 py-1.5 text-xs text-[#8b8b8b] uppercase tracking-wider">
            Commands
          </div>
          {commands.map((cmd, i) => (
            <div
              key={i}
              className="flex items-center justify-between px-4 py-2 hover:bg-[#5e6ad2]/10 cursor-pointer transition-colors"
            >
              <div className="flex items-center gap-3">
                <span className="w-6 h-6 flex items-center justify-center text-[#5e6ad2] font-mono text-sm">
                  {cmd.icon}
                </span>
                <span className="text-white text-sm">{cmd.label}</span>
              </div>
              <kbd className="px-2 py-0.5 text-xs text-[#8b8b8b] bg-[#2a2a2c] rounded font-mono">
                {cmd.shortcut}
              </kbd>
            </div>
          ))}
        </div>

        {/* Footer */}
        <div className="flex items-center justify-between px-4 py-2 border-t border-[#2a2a2c] text-xs text-[#8b8b8b]">
          <div className="flex items-center gap-4">
            <span className="flex items-center gap-1">
              <kbd className="px-1 py-0.5 bg-[#2a2a2c] rounded text-[10px]">tab</kbd>
              <span>to navigate</span>
            </span>
            <span className="flex items-center gap-1">
              <kbd className="px-1 py-0.5 bg-[#2a2a2c] rounded text-[10px]">enter</kbd>
              <span>to select</span>
            </span>
          </div>
        </div>
      </div>
    </div>
  );
}

<LinearCommandMenu />
