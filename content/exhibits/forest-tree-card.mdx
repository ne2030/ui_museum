---
title: Forest Tree Card
slug: forest-tree-card
description: An organic tree visualization with growing branches and leaves
source:
  designer: Nature Studio
  designerUrl: https://nature.design
  company: Forest Design
  originalUrl: https://forest.design
  designSystem: Botanical
theme:
  background:
    type: gradient
    value: "linear-gradient(180deg, #1a2f1a 0%, #0d1f0d 50%, #051005 100%)"
  colors:
    primary: "#4a7c59"
    secondary: "#2d5a3d"
    surface: "#0d1f0d"
    text: "#e8f0e8"
    muted: "#8ab89a"
    border: "#2d4a2d"
    accent: "#7cb87c"
  typography:
    heading:
      family: Georgia
      weight: 400
    body:
      family: Georgia
      weight: 400
    mono:
      family: Courier New
      weight: 400
  radius:
    style: rounded
    values:
      sm: "8px"
      md: "16px"
      lg: "24px"
      full: "9999px"
  effects:
    shadows: soft
    glow:
      enabled: true
      color: "#4a7c59"
      spread: 30
  interactions:
    hover:
      scale: 1.02
      timing: 400
    click:
      feedback: none
  animations:
    enter:
      type: fade
      duration: 800
  meta:
    service: Forest
    designSystem: Botanical
    era: "2024"
    philosophy: "Organic growth patterns, earthy colors, and natural motion. Design inspired by the patience and beauty of nature."
    changes: "Evolved from flat illustrations to layered depth with animated foliage."
tags:
  - nature
  - tree
  - organic
  - animation
category: cards
featured: true
---

export function ForestTreeCard() {
  return (
    <div className="relative w-full max-w-md mx-auto">
      {/* Floating leaves background */}
      <div className="absolute inset-0 overflow-hidden pointer-events-none">
        {[...Array(12)].map((_, i) => (
          <div
            key={i}
            className="absolute text-2xl animate-float"
            style={{
              left: `${10 + (i * 7) % 80}%`,
              top: `${5 + (i * 13) % 90}%`,
              animationDelay: `${i * 0.5}s`,
              animationDuration: `${4 + (i % 3)}s`,
              opacity: 0.6,
            }}
          >
            {i % 3 === 0 ? '🍃' : i % 3 === 1 ? '🌿' : '🍂'}
          </div>
        ))}
      </div>

      {/* Main tree card */}
      <div className="relative bg-gradient-to-b from-[#1a3a1a] to-[#0d1f0d] rounded-3xl p-8 border border-[#2d4a2d] shadow-2xl">
        {/* Tree illustration */}
        <div className="flex flex-col items-center mb-6">
          {/* Tree crown */}
          <div className="relative">
            <div className="w-32 h-24 bg-gradient-to-t from-[#2d5a3d] to-[#4a7c59] rounded-full transform -translate-y-2" />
            <div className="absolute top-4 left-4 w-28 h-20 bg-gradient-to-t from-[#3d6a4d] to-[#5a8c69] rounded-full" />
            <div className="absolute top-2 left-8 w-20 h-16 bg-gradient-to-t from-[#4d7a5d] to-[#6a9c79] rounded-full" />
          </div>
          {/* Tree trunk */}
          <div className="w-6 h-16 bg-gradient-to-b from-[#5a4a3a] to-[#3a2a1a] rounded-b-lg -mt-4" />
        </div>

        {/* Card content */}
        <div className="text-center">
          <h3 className="text-xl font-serif text-[#e8f0e8] mb-2">
            Oak Tree
          </h3>
          <p className="text-sm text-[#8ab89a] mb-4 font-serif italic">
            Quercus robur
          </p>

          {/* Stats */}
          <div className="grid grid-cols-3 gap-4 mb-6">
            <div className="text-center">
              <div className="text-2xl text-[#7cb87c]">127</div>
              <div className="text-xs text-[#8ab89a]">Years</div>
            </div>
            <div className="text-center">
              <div className="text-2xl text-[#7cb87c]">24m</div>
              <div className="text-xs text-[#8ab89a]">Height</div>
            </div>
            <div className="text-center">
              <div className="text-2xl text-[#7cb87c]">892</div>
              <div className="text-xs text-[#8ab89a]">Leaves</div>
            </div>
          </div>

          {/* Action button */}
          <button className="w-full py-3 px-6 bg-[#4a7c59] hover:bg-[#5a8c69] text-white rounded-xl transition-colors font-serif">
            View Growth History
          </button>
        </div>
      </div>

      {/* Ground decoration */}
      <div className="flex justify-center gap-2 mt-4">
        <span className="text-lg">🌱</span>
        <span className="text-lg">🌿</span>
        <span className="text-lg">🌱</span>
      </div>
    </div>
  );
}

<ForestTreeCard />
